# Makefile to install Python modules under $ANF
# Doesn't work on Mac due to scipy being silly.
#
# REQUIRES: numpy scipy (anfsrc)
# REQUIRES: matplotlib  (Antelope)

MODULE_NAME = spectrum
MODULE_VERSION = '>=0.6.0'

include ../lib/pymodule.mk
